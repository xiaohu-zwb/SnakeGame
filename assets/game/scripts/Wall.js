const BaseView = require("BaseView");

cc.Class({
    extends: BaseView,

    properties: {
        
    },

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {},

    start () {

    },

    init(posX, posY) {
        this._super(posX, posY);
        return this;
    },

    // update (dt) {},
});
